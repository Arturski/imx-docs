---
id: "error-codes"
title: "API error codes"
slug: "/error-codes"
sidebar_position: 1
---

## contract_already_exists

컬렉션이 이미 등록되었습니다. 하나의 컬렉션은 한 번만 등록될 수 있습니다. [컬렉션 등록 API](/reference#/operations/createCollection)에 전달된 `contract_address` 필드의 값이 올바른지 확인하십시오.

이전에 컬렉션을 등록했고 그 세부 내용의 일부를 업데이트하고자 하는 경우, [컬렉션 업데이트 API](/reference#/operations/updateCollection) 문서를 참조하십시오.

## missing_metadata_key

*필요한* 최소 하나의 메타데이터 키가 요청에 제공되지 않았습니다. 오류 응답의 `message` 필드가 누락된 특정 메타데이터 키(들)를 지정합니다. 예시:

```json
{
  "code": "missing_metadata_key",
  "message": "Missing required metadata key: name"
}
```

## metadata_key_already_exists

컬렉션에 추가하려는 메타데이터가 이미 존재합니다. 컬렉션 메타데이터를 업데이트하려면 [컬렉션 메타데이터 업데이트 API](/reference#/operations/updateCollection)를 참조하십시오.

## mint_validation_failed

민팅 요청 페이로드가 인증에 실패했습니다. `message` 필드에 인증 오류에 대한 더 자세한 내용이 있습니다.

## asset_invalid_id
`/assets/{address}/{id}` - 컨트랙트 주소와 토큰 ID는 고유한 복합 키입니다.  
컨트랙트 주소는 헥스(예: '0x02311ab2...')가 아니며 토큰 ID도 정수가 아닙니다. 두 매개 변수 모두 가변 문자 필드로 DB에 저장됩니다.

## asset_not_found

`/assets/{address}/{id}` - 컨트랙트 주소와 토큰 ID는 고유한 복합 키입니다. 그러한 ID를 갖는 자산을 데이터베이스에서 찾지 못했습니다(imx_nft + imx_collection + imx_royalty의 조합)

## asset_invalid_format

DB의 응답 `[]store.Asset`이 `[]api.Asset`로 변환되지 못했습니다.

## invalid_mint_id

제공된 민팅 `id`가 유효하지 않습니다. 유효한 정수로 올바르게 제공되지 않았거나, 정수 오버플로우, 음의 정수 등 `id`를 분석하는 데 다른 문제가 있었습니다.

## mint_not_found

제공된 민팅 `id`는 유효하지만 존재하지 않습니다. 민팅된 토큰의 `id`를 제공하여 주십시오.

## mint_unwithdrawable

민팅을 시도 중인 토큰이 이후 L1 레이어에서 인출될 수 있게 하기 위한 인증을 전달하지 않았습니다. 이것은 흔히 토큰 ID/블루프린트와 스마트 컨트랙트 `mintFor` 로직 사이의 불일치로 인해 발생합니다.

이 오류는 시도된 민팅의 토큰 주소, ID, 블루프린트를 기록합니다.

## unique_project_error

제공된 요청 객체가 다른 프로젝트에 속한 컬렉션 주소를 포함하고 있습니다. 제공되는 컬렉션 주소가 같은 프로젝트에 속하도록 하면 이 오류가 해결됩니다.

## mint_limit_exceeded

민팅 요청이 주어진 시간 동안 프로젝트당 허용된 민팅 횟수를 초과하였습니다. 프로젝트의 한도를 늘리려면 고객 지원팀에 연락하십시오.

## collection_limit_exceeded

컬렉션 생성 요청이 주어진 시간 동안 생성된 프로젝트당 허용된 컬렉션 개수를 초과하였습니다. 프로젝트의 한도를 늘리려면 고객 지원팀에 연락하십시오.